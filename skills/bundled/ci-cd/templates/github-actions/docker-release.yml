name: Build and Publish Image

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: image-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write
  id-token: write

env:
  IMAGE_NAME: ghcr.io/OWNER/REPO

jobs:
  image:
    name: Build, scan, and publish
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@REPLACE_WITH_PINNED_SHA
        with:
          persist-credentials: false

      - uses: docker/setup-buildx-action@REPLACE_WITH_PINNED_SHA

      - uses: docker/login-action@REPLACE_WITH_PINNED_SHA
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@REPLACE_WITH_PINNED_SHA
        id: meta
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=sha,format=long,prefix=sha-
            type=raw,value=main,enable={{is_default_branch}}

      - uses: docker/build-push-action@REPLACE_WITH_PINNED_SHA
        id: build
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

      - name: Scan image
        uses: aquasecurity/trivy-action@REPLACE_WITH_PINNED_SHA
        with:
          image-ref: ${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}
          severity: HIGH,CRITICAL
          # Reference-only until a baseline artifact or tracked issue lists confirmed exploitable findings with owners, dispositions, and target dates; then switch to exit-code: "1".
          exit-code: "0"
