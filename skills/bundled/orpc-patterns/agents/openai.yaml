interface:
  display_name: "oRPC Fullstack"
  short_description: "Build and review oRPC APIs"
  default_prompt: "Use $orpc-fullstack to review this oRPC API change for current patterns, missing validation, typed errors, and client/query integration issues."
policy:
  allow_implicit_invocation: true
