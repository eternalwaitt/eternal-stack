# Quality

- Verify before claiming completion.
- Fix code instead of loosening linters, TypeScript, hooks, or test gates.
- Avoid silent fallbacks, empty catches, placeholder comments, and hidden defaults.
- Keep changed code small, typed, and aligned with local patterns.
- After meaningful edits, run the plan's final verification gate or the smallest project preflight that proves the behavior.
