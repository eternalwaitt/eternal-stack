# Domains

- Invoke `eternal-best-practices` before editing domain-sensitive surfaces when installed.
- Domain-sensitive surfaces include auth, tenancy, billing, payments, money values, i18n, Prisma/schema, permissions, and soft deletes.
- Use companion skills when available: `eternal-best-practices`, `code-simplifier`, `finding-duplicate-functions`, `brooks-audit`, and domain-specific skills (`domain-*`, `better-auth`, `tenant-isolation`, `money-vo-discipline`, `prisma-expert`, `i18n-localization`, `stripe-best-practices`, `abacatepay-integration`).
- If a companion skill is unavailable, state that explicitly and run the closest manual review pass.
