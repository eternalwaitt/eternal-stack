# Eternal Stack Coverage

Status key: `done` means implemented in this repo; `live-gated` means intentionally controlled by local install/canary because it mutates a personal Claude setup.

| Planned area | Current coverage |
| --- | --- |
| Hook library and state | done: JSON, state, paths, preflight, code policy, complexity, cleanup, and event-extract helpers. |
| Observer hooks | done: prompt routing, prompt expansion, `CLAUDE.md` reinjection, advisory locked rate limiting, post-tool batch observation, and session cleanup. Reference: `docs/hooks.md` (catalog), `docs/guards.md` (deny rules). |
| Strict blockers | done: Bash, directory `Read`, shell output-limiter, edit/write, WebSearch, agent/task, evidence-first, stale verification, requested-skill, domain-skill, first-failure context, repeated-failure, Stop, and SubagentStop gates. |
| Compact recovery | done: PreCompact/PostCompact state with timestamp/count metadata plus SessionStart recovery, workflow status, and skill hints. PreCompact also writes a lossless session snapshot whose path rides the `compact_pre` event. |
| Automatic retro and steering | done: `etrnl-retro.mjs` distills rules-only lessons from state events at SessionEnd/PreCompact (fail-open), injects a bounded top-lesson index at SessionStart/PostCompact, prunes with confidence decay, honors project-local `.etrnl/STEERING.md` once per update, and retains `env_remedy`/`recurring_defect` lessons to Hindsight (canary-gated) with `reflect()` when supported. Reference: `docs/hooks.md` (Retro and steering). |
| Review convergence | done: parallel reviewers merge once through `review-merge.mjs` (fingerprint dedup, confidence gates, `autofix_class` routing); fixes cap at two rounds per wave with a stall rule; reopen only on P0/P1; tier ≤2 uses one merged pass per wave plus a plan-end adversarial pass; deterministic `review-rules.mjs` matches never reach LLM review. |
| Review economy | done: named park thresholds stop a stream before the reopen cap is spent — `recurringFindingCount` ≥ 3, `streamAlternationCount` ≥ 4, `roundsSinceProgress` ≥ 2 — each overridable by env and fed by `execution-ledger.mjs record-trajectory --wave`. `review-rules.mjs check --report-only` reports without escalating a block-mode match and exits 0. `review-merge.mjs skip-plan` auto-skips a reviewer after five consecutive zero-finding dispatches, counting them in an additive `reviewerDispatches` key inside the existing `review-learnings.json` rather than a second store; security lenses, tenancy lenses, and every deep-audit lane are exempt, with the lane list derived live from the deep-audit registry and an unloadable registry reporting `skipEvaluation: unavailable` so nothing skips. Every skip carries a machine-readable `reasonCode`. Tier ≤2 defers mid-wave human-verify pauses to wave or phase close; tier 3 keeps explicit UAT gates. Reference: `skills/etrnl-dev-execute/references/bounded-review.md`, `references/batch-execution.md`. |
| Codex model routing | done: `scripts/lib/codex-model-routing.mjs` resolves `{ model, reasoningEffort }` from a `modelTier` (`fast` → `gpt-5.6-luna`/low, `standard` → `gpt-5.6-terra`/medium, `top` → `gpt-5.6-terra`/high), honoring packet fields, then `ETRNL_CODEX_MODEL_<TIER>`, then the static map, and throwing rather than inheriting. `gpt-5.6-sol` needs a justification naming an integration-owner or adversarial escalation. `agent-task-packet-check.mjs` errors on a write packet with no `codexModel` and templates `read-only`/`write`/`mini` emit the resolved slug. |
| Deep-audit lane routing | done: every lane across `ui-ux-product` and `performance` declares a `modelTier` validated when the registry loads, so a lane cannot be constructed without one; `resolveLaneDispatch` and `categoryLaneDispatch` emit the explicit model and effort per lane spawn. Eleven lanes resolve to seven at `gpt-5.6-terra`/medium and four at `gpt-5.6-luna`/low, none inheriting. A registry test iterates categories dynamically so a future category cannot reintroduce inherit. |
| Plan scope triage | done: `diff-triviality.mjs classify-plan --plan <path> [--json]` grades a plan's file map Trivial, Small, or Large, and autoplan carries the result in its `## Tier assessment` section. Trivial requires tier ≤2, ≤3 files, and no behavioral, API, or schema change; classification fails safe toward `large` on an unreadable plan, an empty file map, or a row silent about behavior, and tier-3 escalation paths return `large` by path so an under-declared `Risk tier:` cannot buy a lighter shape. |
| Execute host profile | done: `ETRNL_EXECUTE_HOST=codex` (auto-detected when unset) runs two concurrent lanes and one merged review per wave for tier ≤2; tier 3 keeps all three reviewer roles with the per-task chain on wave 1 and merged review from wave 2 unless the plan names full fan-out. Claude keeps three lanes and its existing behavior. Rolling hour ETAs, wall-clock finish times, and elapsed-time percentages are prohibited on every host; status comes from `execution-ledger.mjs history --gates`. |
| Plan-time convergence | done: tier ≥3 plans run a bounded spec → quality → adversary loop capped at three cycles, stalling when the open-high count stops falling, and hand off only at Final or a logged owner override. Tier ≤2 plans skip it. Reference: `skills/etrnl-dev-autoplan/references/plan-review-convergence.md`. |
| Hook hygiene | done: `cc-compact-suggest.sh` is a PreToolUse advisory at a window-scaled token threshold, skipped under `ETRNL_HOOK_PROFILE=minimal`; `cc-question-preference.sh` denies `AskUserQuestion` and MCP ask tools under a `never-ask` preference, with a one-way-door clamp for deploy, production schema, destructive data, auth, and money evaluated before any preference lookup so no file entry or env override can bypass it. The prompt router fingerprints hints to stop repeat injection, and `ETRNL_SKIP_HOOKS` disables named hooks without the full minimal profile. Reference: `docs/hooks.md` (catalog), `docs/guards.md` (fail-open matrix). |
| Efficiency measurement | done: `scripts/codex-rollout-baseline.mjs` aggregates a parent Codex rollout plus every subagent rollout it spawned into wall duration, parent-vs-subagent billed tokens, compaction count, spawn count, explicit-vs-inherited spawn models, and turn-model distribution; `baseline` and `trend` persist under `${ETRNL_ARTIFACTS_DIR}/codex-baselines/` on the `ux-audit-check.mjs` convention, so routing changes are measured rather than asserted. Reference: `docs/health-stack.md`. |
| Startup prompt budget | done: `prompt-budget-check.mjs` caps each `skills/*/SKILL.md` at 18 000 bytes and agents at 14 000, and doctor fails hard on a breach. Depth that would breach the cap moves into on-demand `references/` modules instead of raising the limit, keeping always-on session context flat as workflows grow. |
| Skill set | done: `etrnl-*` orchestration family, canonical `/etrnl-dev-ci`, `etrnl-frontend-patterns` reference orchestrator (DESIGN.md workflow, brand presets, motion, design-review rubric), plus bundled policy/review/domain/frontend skills documented in `docs/skills.md`. |
| Frontend design generation | done: bundled `frontend-design` (baseline visual direction), `impeccable` (product-UI craft/critique/audit fork), and `design-taste-frontend` (landing/marketing taste); disambiguated by `etrnl-frontend-patterns` and `etrnl-router`. |
| Accessibility depth | done: bundled `wcag-accessibility` for explicit WCAG/a11y asks; `etrnl-deep-audit-ux` remains the whole-product UI audit surface. |
| UI/UX audit exhaustiveness | done: `scripts/ux-inventory.mjs` supplies route/surface/state/axis denominators and a mechanical anti-pattern scan, `scripts/ux-audit-check.mjs coverage` fails sampled runs without a `coverageExceptions` reason, and `etrnl-deep-audit-ux` overrides the borrowed "top 3-5 issues" caps with per-check `uxHealthScore`, `opportunity` findings, quick wins, and run-over-run trend. |
| UX research | done: bundled `ux-researcher-designer` for personas, journey mapping, and usability-test frameworks. |
| DESIGN.md workflow | done: `skills/etrnl-frontend-patterns/references/design-md-workflow.md` plus MIT brand presets under `references/design-presets/`; `etrnl-design-reviewer` and `etrnl-dev-autoplan` check for repo `DESIGN.md` before inventing direction. |
| Motion and interaction patterns | done: `skills/etrnl-frontend-patterns/references/motion-interaction.md` (adapted from emilkowalski/skills, MIT). |
| Per-dimension design review | done: `skills/etrnl-frontend-patterns/references/design-review-rubric.md` with 0–10 scoring per dimension; `etrnl-design-reviewer` replaces single completeness score with rubric-backed dimensions. |
| React UI execute gate | done: `etrnl-dev-execute` runs `react-doctor --diff` on ledger-scoped React/Next UI changes when available; findings on task-changed files are fail-closed (availability stays fail-open). |
| Code health | done: master code-health router, deterministic inventory helper, Health Stack doc, no-skips ledger contract, and bundled-skill audit routing. |
| Writing plans flow | done: file-backed draft, review, improve, final, short chat summary, Hybrid Deep Stack metadata, and `/etrnl-dev-autoplan` full-depth planning with CEO, engineering, DX, adversarial, specialist, reuse, simplifier, and findings convergence. |
| Execution flow | done: phase gates, Hybrid risk tiers only after deep review, optional phase/workstream/UAT ledger metadata, schema v2 execution events/reviews, structured task packets with task identity, lineage, packet hashes, reuse/TDD/simplifier evidence fields, wave planning, overlap checks, worktree eligibility, deep-stack packet ownership fields, `etrnl-dev-execute` completion blocking when packet-bound implementation/reviewer/TDD/simplifier/reuse/TypeScript/install evidence is missing, mandatory spec/quality/simplifier reviewer evidence, critical review, durable artifacts, verification, final simplification/dedupe/domain passes, and no-pause question policy. |
| Durable artifacts | done: deep-stack bundle, sanitized source manifest, skill matrix, review phase records, TDD evidence, reuse inventory and reuse bindings, findings ledger, completion audit and completion reconciliation, TypeScript trigger evidence, install proof, Hybrid risk tier, review log, browser QA v1/v2 reports with mandatory console/network summaries, route/viewport matrix counts, screenshot hashes, capture freshness, and provenance for completed v2 runs, context save/restore, artifact-required ledger checks, redacted cross-session project buglog hints, workflow-health summaries/status JSON, and local tool-effectiveness verdict summaries. |
| Agent templates | done: 11 default-installed `etrnl-*` agents — `etrnl-executor` (implementation), `etrnl-spec-reviewer` and `etrnl-quality-reviewer` (spec/quality review), `etrnl-design-reviewer` (per-dimension rubric design review with `DESIGN.md` check) and `etrnl-dx-reviewer` (design/DX review), `etrnl-investigator` and `etrnl-scout` (discovery), `etrnl-adversary` (adversarial review), `etrnl-browser-qa` (browser QA with design-evidence taxonomy), `etrnl-consumer-tracer` (call-site coverage after shared-value changes), and `etrnl-test-wiring-auditor` (test-wiring check for behavioral diffs). |
| Shared startup guidance | done: public `AGENTS.md` template plus tiny Claude wrapper importing it. |
| Rules | done: namespaced `rules/etrnl/*.md` to avoid clobbering existing user rules; cross-host eternal-saas pack (`rules/eternal-saas/global/` + `rules/eternal-saas/project/`) with `rules-manifest.json` (schema v1, checksums, privacy gates); `scripts/init-project-rules.sh` deploys pack to any target repo with Claude+Cursor outputs; `scripts/sync-rule-exports.mjs` keeps `.mdc` Cursor twins in sync; doctor byte gate prevents Codex startup overflow. Reference: `docs/rules.md`. |
| Install/update/rollback/doctor | done: scripts, tests, rollback (including `rules/eternal-saas` + Codex startup file restore), canaries, rules, docs, templates, agents, settings audit/repair, installed update metadata, drift explain output, artifact helpers, post-upgrade browser-QA canary, installed-home doctor, and workflow-health helpers are installed or checked. |
| Hindsight memory consolidation | live-gated: canary verifies strict config; actual migration/removal of competing memory systems remains a personal live operation. |
| Plugin/MCP cleanup | live-gated: plan requires inventory and explicit local rollout before removing plugins, MCPs, or permissions. |
| Shareable repo boundary | done: public templates exclude private identity, accounts, transcripts, secrets, and memories. |
| Release and public docs | done: `VERSION`, Keep a Changelog `CHANGELOG.md`, `docs/RELEASING.md`, `scripts/release.mjs`, `scripts/changelog-release-check.mjs`, `README.md`, `CREDITS.md`, root `AGENTS.md`/`CLAUDE.md`, and aligned `docs/skills.md` / `docs/health-stack.md`. |

## Bundled skills

Eternal Stack is a bundled skill family: `etrnl-*` orchestration from this repo plus policy, review, and domain skills that install on the host and are routed by hooks and workflows. See `docs/skills.md` for the full inventory. Representative bundled skills:

- `eternal-best-practices`
- `domain-*`
- `better-auth`
- `tenant-isolation-patterns`
- `money-vo-discipline`
- `prisma-expert`
- `i18n-localization`
- `stripe-best-practices`
- `abacatepay-integration`
- `code-simplifier`
- `finding-duplicate-functions`
- `frontend-design`
- `impeccable`
- `design-taste-frontend`
- `wcag-accessibility`
- `ux-researcher-designer`
- Brooks guidance via `etrnl-code-review-excellence/references/` (supersedes standalone `brooks-audit` for stack use)
- Frontend orchestration via `etrnl-frontend-patterns/references/` (DESIGN.md workflow, presets, motion, design-review rubric)

## Best-Of-All-Worlds Layers

| Layer | Status | Deterministic proof |
| --- | --- | --- |
| Skill trigger evals | done | `tests/fixtures/skill-triggering/cases.json`, `tests/test-hooks.sh`, `skill-behavior-smoke.mjs`. |
| Workflow status | done | `workflow-health.mjs status` / `status --json`, SessionStart status hints, and workflow-tool tests for stale, missing artifact, UAT, and text output states. |
| Tool effectiveness | done | `tool-effectiveness.mjs validate-fixtures`, `baseline`, `import-codex`, `summarize`, `ETRNL_TOOL_EFFECTIVENESS_DISABLED=1` kill switch for summarize/import/baseline and workflow-health effectiveness omission, and workflow-health effectiveness projection tests for sanitized CodeGraph, Beads, and hook-pattern signals. |
| Browser QA v2 | done | `browser-qa-report.mjs` validates v2 matrix reports, hashes screenshots, rejects missing/out-of-root/stale screenshot evidence, and post-upgrade canary exercises the installed helper. |
| Local learning hints | done | `project-buglog.mjs suggest --json` and `suggest-project --json`, cross-session fingerprints, redaction, stale filtering, hook debounce, and disable flag tests. |
| Reviewer-gated subagents | done | write packets require task identity and lineage; multi-file write packets require reviewer contracts; Stop blocks execute completion when packet-bound implementation evidence is missing or spec/quality reviewer evidence is absent. |
| Phase/UAT artifacts | done | `execution-ledger.mjs set-phase` and `record-uat`; open UAT findings block `check-stop`. |
| Hybrid Deep Stack artifacts | done | `deep-stack-check.mjs validate-plan --plan <plan>` and section validators require artifact bundles for final plans; legacy transition is an explicit flag, not the default. |
